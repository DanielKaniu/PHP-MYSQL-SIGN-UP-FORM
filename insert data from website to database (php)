<?php

//link with a file that has a code that connects with a mysql database

	include_once 'database _connection.php';
	
//get data from the url after pressing sign up button (below)

	$first = $_POST['first'];
	$last = $_POST['last'];
	$email = $_POST['email'];
	$uid = $_POST['uid'];
	$password = $_POST['pwd'];
	
//insert data through form element into the database	

$sql = "INSERT INTO users (user_first, user_last, user_email, user_uid, user_pwd)
VALUES ('$first', '$last', '$email', '$uid', '$password');";

//a function that quesries the mysql database (below)

mysqli_query($conn, $sql);

//path to go back to the sign up page after signing up

header("Location: ../code.php?signup=imeweza!");

 
?>
